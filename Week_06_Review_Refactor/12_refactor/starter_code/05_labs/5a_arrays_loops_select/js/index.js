/*
Step 1:
create an array called "months" and fill it with 12 strings, one corresponding to the name of each month

Step 2:
using a .forEach() loop, populate the select menu by:
2a) looping through the array and
2b) using the .append() or .prepend() method to add a new <option> element to the <select> element, so that the name of each month appears in the list

Step 3:
Using the .change() method,  and $(this).val(),  detect when a user makes a selection and the selected item changes.  When this happens, display the user's selection on the page

*/


$(document).ready(function(){

	
});
